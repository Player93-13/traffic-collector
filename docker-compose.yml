version: "3.8"
services:
  collector:
    image: ghcr.io/Player93-13/traffic-collector:latest
    restart: unless-stopped
    env_file: .env
    environment:
      - INTERVAL=60
      - HEALTH_PORT=9229
    networks:
      - backend

networks:
  backend:
    driver: bridge